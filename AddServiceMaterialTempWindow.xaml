<Window x:Class="LashAccountingSystem.AddServiceMaterialTempWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление материала" 
        Height="400" Width="400"
        Background="White"
        FontFamily="Roboto"
        Foreground="#3D5736"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="➕ Добавить материал" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#3D5736"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,5">
            <TextBlock Text="Материал:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="MaterialComboBox" 
                      Height="35" 
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="MaterialId"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,5">
            <TextBlock Text="Количество:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="QuantityTextBox" Height="35" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" Width="100" Height="35" 
                    Margin="10" Background="#3D5736" Foreground="White" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="100" Height="35" 
                    Margin="10" Background="#B35D6E" Foreground="White" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>